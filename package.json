{
  "name": "kanjirowa-mart-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Shared backend for Kanjirowa Mart: one account per email (with Gmail alias handling), same password on every device, and an admin dashboard that sees every device's signups and orders.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "bcryptjs": "^2.4.3",
    "cookie-session": "^2.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "nodemailer": "^6.9.14"
  }
}
